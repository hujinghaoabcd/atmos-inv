# External storage roots. Copy to .env locally; never commit .env.
ATMOSINV_DATA_ROOT=/path/to/permanent/atmos-inv
ATMOSINV_SCRATCH_ROOT=/path/to/scratch/atmos-inv
ATMOSINV_RUN_ROOT=/path/to/runs/atmos-inv
ATMOSINV_MLFLOW_URI=file:///path/to/mlruns
